spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://120.78.170.43:3306/practice?useUnicode=true;characterEncoding=utf-8;useSSL=false;serverTimezone = GMT
      username: practice
      password: shootingstar
      initial-size: 10
      max-active: 60
      min-idle: 5
      filters: wall,mergeStat
      filter:
        stat:
          log-slow-sql: true
          slow-sql-millis: 5000
      test-while-idle: true
      test-on-borrow: true
  main:
    allow-bean-definition-overriding: true

#mybatis plus
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080